
if (active == 1)
{		
	/*
	// box movement
	if (mystage == 0)
	{
		box_x = lerp(box_x, (orig_box_x - box_w), 0.2);
		var _dist = abs(box_x - (orig_box_x - box_w));
		if (_dist <= 0.1)
		{
			box_x = (orig_box_x - box_w);
			mytimer = 120;
			mystage = 1;
		}
	}
	
	// box disappear
	if (mystage == 1 && mytimer <= 0)
	{
		box_y -= myscale;
		if (box_y <= -60)
		{
			global.notification[myself] = 0;
			instance_destroy();	
		}
	}
	else if (mytimer > 0)
		mytimer -= 1;	
	*/
}


















