
active = 1;

/*
for (var i = 0; i < global.music_length; i++)
{
	if (global.music[i] != 0 && audio_is_paused(global.music[i]) == 1)
	{
		mus_resume(global.music[i], 0.5);
		
		audio_resume_sound(global.music[i]);
		mus_volume(global.music[i], 0, 0);
		mus_volume(global.music[i], 1, 0.5);
	}
}
*/
